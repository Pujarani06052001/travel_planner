import Navbar from './navbar';

const Book_now = () => {
    return (
        <div>
            <Navbar />
            <h1>Book Now Page</h1>
            <p>Book your trip now.</p>
        </div>
    );
};

export default Book_now;
