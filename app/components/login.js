import Navbar from './navbar';

const Login = () => {
    return (
        <div>
            <Navbar />
            <h1>Login Page</h1>
            <p>Login to your account.</p>
        </div>
    );
};

export default Login;
